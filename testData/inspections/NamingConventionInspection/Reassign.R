# assign
<weak_warning descr="Variable names should contains only lowercase letters, numbers, and underscores">aBaCAba</weak_warning> <- 42

# reassign
aBaCAba <- 43

# usage
if (aBaCAba == 42) {
  print(aBaCAba)
}